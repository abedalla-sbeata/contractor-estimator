/**
 * Contractor self-serve portal (register / login / dashboard / billing).
 * Disabled: Admin creates contractor accounts via API; contractors only share client links.
 * Set to true to re-enable the old portal UI without restoring deleted code.
 */
export const CONTRACTOR_PORTAL_ENABLED = false;
